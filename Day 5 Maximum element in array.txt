#include<stdio.h>
int main(){
   int arr[100],n,i,max;
   printf("Enter number of element:");
   scanf("%d",&n);
   printf("Enter %d elements:\n",n);
   for(i=0;i<n;i++){
       scanf("%d",&arr[i]);
   }
   max=arr[0];
   for(i=1;i<n;i++){
       if(arr[i]>max)
       max=arr[i];
   }
   printf("Maximum element=%d\n",max);
   return 0;
}

Output

Enter number of element:2 3 45 
Enter 2 elements:
Maximum element=45


=== Code Execution Successful ===